<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:paddingBottom="@dimen/widget_vertical_margin"
    android:paddingLeft="@dimen/widget_horizontal_margin"
    android:paddingRight="@dimen/widget_horizontal_margin"
    android:paddingTop="@dimen/widget_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    >

    <TextView
        android:id="@+id/widget_textview_nothing_to_show"
        android:visibility="gone"
        android:text="@string/activity_main_nothing_to_show"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/text_color"/>

    <ListView android:id="@+id/data_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:dividerHeight="@dimen/widget_list_view_divider_height"
        android:divider="@color/transparent"
        android:orientation="horizontal"/>

</RelativeLayout>